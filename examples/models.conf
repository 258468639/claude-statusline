# Example: model_id -> alias mapping
# Lines starting with # are comments.
# Format:  <model_id> = <alias>
#
# Find your current model_id by running Claude Code with --debug, or by
# inspecting the JSON piped into the script (`.model.id`).

claude-opus-4-8    = glm-5.1
claude-sonnet-4-6  = DeepSeek-V4
claude-haiku-4-5   = kimi-k2.6
